# Core
pydantic>=2.0

# Hugging Face (optional — needed only for --use-hf mode)
transformers>=4.38.0
torch>=2.0.0
accelerate>=0.27.0
protobuf>=3.20.0

# API (for HF Spaces)
fastapi>=0.110.0
uvicorn>=0.29.0
